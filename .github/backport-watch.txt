# Paths watched by .github/scripts/check-backports.sh
#
# Only list files that are meant to stay near-identical on every branch.
# Feature code does not belong here: it diverges on purpose and would drown
# the report in false positives.
#
# One path per line. Blank lines and lines starting with # are ignored.

docker-entrypoint.sh
Dockerfile
DOCKER.md
DOCKER-SECRETS.md
