# API Testing Environment Configuration
# Copy this file to .env.api-test and configure for your testing environment

# API Configuration
API_BASE_URL=http://127.0.0.1:8081
API_KEY=your-api-key-here

# Database Configuration for Testing
DB_HOST=localhost
DB_NAME=poweradmin_test
DB_USER=poweradmin_test_user
DB_PASS=poweradmin_test_password
DB_TYPE=mysql

# Alternative database configurations:
# For PostgreSQL:
# DB_TYPE=pgsql
# For SQLite:
# DB_TYPE=sqlite
# DB_NAME=/path/to/test.db

# Test Configuration
TEST_TIMEOUT=30
TEST_CLEANUP=true

# Load Testing Configuration
LOAD_TEST_CONCURRENT=10
LOAD_TEST_TOTAL=100
LOAD_TEST_DURATION=60
LOAD_TEST_RAMP_UP=10

# Optional: HTTP Basic Auth for API testing (if enabled)
HTTP_AUTH_USER=testuser
HTTP_AUTH_PASS=testpass

# Optional: Dynamic DNS Testing
DYNAMIC_DNS_USER=ddns_user
DYNAMIC_DNS_PASS=ddns_password
DYNAMIC_DNS_HOSTNAME=test.example.com
EOF < /dev/null